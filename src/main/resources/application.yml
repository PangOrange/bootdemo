server:
  port: 8888
  servlet:
    context-path:
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dbOrange?useSSL=true
    username: root
    password: 295683
  mvc:
    view:
      prefix: /WEB-INF/html/
      suffix: .html
  security:
    user:
      name: user
      password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    database: 0
    host: localhost
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        min-idle: 0
        max-idle: 8
        max-wait: 1000
    timeout: 1000